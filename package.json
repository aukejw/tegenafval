{
  "name": "tegenafval",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "BASE_PATH=/tegenafval/ npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "@astrojs/mdx": "^4.3.0",
    "astro": "^5.10.1"
  },
  "devDependencies": {
    "gh-pages": "^6.2.0"
  }
}
